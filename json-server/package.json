{
  "name": "json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "nodemon index.js",
    "format": "yarn prettier --write ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.16.1"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "prettier": "^2.1.2"
  }
}
